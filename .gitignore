# Local-only docs: recon notes and design spec contain real project /
# location / user IDs from the captured session. Kept locally for reference
# but not for the public repo.
docs/

# Raw payload captures from Playwright recon (JWTs, IDs, real content).
recon-artifacts/

# Standard
node_modules/
dist/
*.log
.DS_Store
.vscode/
.idea/
